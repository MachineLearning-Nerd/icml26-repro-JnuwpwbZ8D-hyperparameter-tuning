# C3 method

The certificate checks the optimistic-infimum predicate, both quantifier
blocks, the full atom count, and the asymptotic absorption into the source's
`M_tot`. An independent 128-case checker uses no finite optimization grid.
