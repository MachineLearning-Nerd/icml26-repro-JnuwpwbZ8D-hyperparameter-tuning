# C4 — Theorem 7.2

Verdict: **FALSIFIED AS PRINTED**. A bounded quadratic training objective has
the unique affine path `theta*=alpha`; an affine tuning objective then gives
`M_total=Delta_total=1`. The displayed RHS is zero, but the resulting loss
class pseudo-shatters `p` coordinate instances. The old path-composition audit
is retained only as a superseded derivation check.
