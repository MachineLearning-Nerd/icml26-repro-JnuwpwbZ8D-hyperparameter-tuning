# C6 method

Full rank makes the dual quadratic strictly convex on the `d-1` difference
coordinates. Each coordinate has lower-active, free, or upper-active state.
The checker bounds the path description by `(p+1)3^p`, substitutes `p=d-1`
into C4, and verifies the `O(d^2)` reduction.
