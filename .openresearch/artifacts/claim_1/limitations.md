# C1 limitations and deviations

This is an exact falsification of the literal displayed bound, not an empirical
pseudo-dimension estimate. It does not refute the intended guarded statement,
such as replacing the complexity logarithms by `log(1+z)` or `log(max(2,z))`.

The retained derivation audit does not re-formalize the several-hundred-page
Basu-Pollack-Roy
It does not re-formalize the several-hundred-page Basu-Pollack-Roy
quantifier-elimination algorithm or the complete Bartlett-Indyk-Wagner GJ
theorem from axioms. Instead, it explicitly treats their published theorem
statements—the same dependencies cited by the paper—as premises and
mechanically verifies the paper's claimed corollary.

The counterexample needs neither external theorem. It uses only the paper's
FOL definition, the printed formula, and the definition of pseudo-shattering.
