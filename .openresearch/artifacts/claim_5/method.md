# C5 method

The verifier checks the signed-lift control, reconstructs the two-block FOL,
and mechanically simplifies the C1 result to `O(p^3 d+p^2 d^2)`. Twelve
widely separated `(p,d)` cases independently check the coefficient witness.
